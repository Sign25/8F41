# 🚀 Инструкция по развертыванию на хостинге REG.RU

Подробное руководство по установке Markdown Document Converter на хостинге REG.RU с поддержкой Python 3.9.

---

## 📋 Требования

### Хостинг REG.RU:
- ✅ Тариф с поддержкой Python 3.9
- ✅ SSH доступ (желательно, но не обязательно)
- ✅ Панель управления ISPmanager или аналог
- ✅ Минимум 1 GB свободного места

### Системные требования:
- Python 3.9 или выше
- Поддержка Passenger WSGI
- Доступ к pip для установки пакетов

---

## 🔧 Шаг 1: Подготовка проекта

### 1.1. Загрузка файлов на сервер

Подключитесь к серверу через FTP/SFTP (FileZilla, WinSCP и т.д.):

```
Хост: ваш-домен.ru
Порт: 21 (FTP) или 22 (SFTP)
Логин: ваш логин от REG.RU
Пароль: ваш пароль
```

Загрузите все файлы проекта в директорию:
```
/home/u1234567/public_html/md/
```

**Замените `u1234567` на ваше реальное имя пользователя!**

### 1.2. Структура после загрузки

```
/home/u1234567/public_html/md/
├── index.html
├── css/
│   └── style.css
├── js/
│   ├── app.js
│   └── roboto-font.js
├── server.py
├── passenger_wsgi.py   ← ВАЖНО!
├── .htaccess            ← ВАЖНО!
├── requirements.txt
└── README.md
```

---

## 🐍 Шаг 2: Настройка виртуального окружения Python

### 2.1. Подключение по SSH

Откройте терминал (или PuTTY на Windows) и подключитесь:

```bash
ssh u1234567@ваш-домен.ru
```

### 2.2. Создание виртуального окружения

```bash
# Перейдите в домашнюю директорию
cd ~

# Создайте виртуальное окружение с Python 3.9
python3.9 -m venv virtualenv/md

# Активируйте окружение
source virtualenv/md/bin/activate

# Обновите pip
pip install --upgrade pip
```

### 2.3. Установка зависимостей Python

```bash
# Перейдите в директорию проекта
cd ~/public_html/md/

# Установите зависимости
pip install -r requirements.txt
```

**Ожидаемый вывод:**
```
Successfully installed Flask-3.0.0 flask-cors-4.0.0 WeasyPrint-60.2 Pillow-10.1.0
```

⚠️ **Возможные проблемы:**

#### Проблема 1: Ошибка установки WeasyPrint
```bash
# Установите системные зависимости (если есть root доступ)
sudo apt-get install libpango-1.0-0 libpangoft2-1.0-0 libffi-dev

# Или обратитесь в поддержку REG.RU для установки:
# - libpango-1.0-0
# - libpangoft2-1.0-0
```

#### Проблема 2: Нет доступа к pip
```bash
# Используйте python3.9 -m pip вместо pip
python3.9 -m pip install -r requirements.txt
```

---

## ⚙️ Шаг 3: Настройка конфигурационных файлов

### 3.1. Обновление passenger_wsgi.py

Откройте файл `passenger_wsgi.py` и **замените `u1234567`** на ваше имя пользователя:

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import sys
import os

# ВАЖНО: Замените u1234567 на ваше имя пользователя!
INTERP = os.path.expanduser("~/virtualenv/md/bin/python3")
if sys.executable != INTERP:
    os.execl(INTERP, INTERP, *sys.argv)

sys.path.insert(0, os.path.dirname(__file__))

from server import app as application
```

### 3.2. Обновление .htaccess

Откройте файл `.htaccess` и **замените `u1234567`** на ваше имя пользователя:

```apache
PassengerEnabled on
PassengerAppRoot /home/u1234567/public_html/md
PassengerPython /home/u1234567/virtualenv/md/bin/python3

SetEnv PYTHON_ENV production
SetEnv FLASK_ENV production

# Статические файлы обрабатываются напрямую
<FilesMatch "\.(html|css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf)$">
    PassengerEnabled off
</FilesMatch>
```

### 3.3. Установка правильных прав доступа

```bash
# Права на passenger_wsgi.py
chmod 755 ~/public_html/md/passenger_wsgi.py

# Права на server.py
chmod 644 ~/public_html/md/server.py

# Права на .htaccess
chmod 644 ~/public_html/md/.htaccess
```

---

## 🌐 Шаг 4: Настройка домена/поддомена

### Вариант 1: Через панель управления ISPmanager

1. Войдите в панель ISPmanager
2. Перейдите в **WWW → Домены**
3. Нажмите **Создать → Поддомен** (или используйте существующий)
4. Укажите:
   - **Поддомен:** md
   - **Основной домен:** ваш-домен.ru
   - **Корневая директория:** /md
5. Нажмите **OK**

Теперь сайт будет доступен по адресу: `https://md.ваш-домен.ru`

### Вариант 2: Использование основного домена

Если хотите использовать основной домен:

1. Скопируйте все файлы из `/md/` в `/public_html/`
2. Обновите пути в `.htaccess`:

```apache
PassengerAppRoot /home/u1234567/public_html
```

Сайт будет доступен по адресу: `https://ваш-домен.ru`

---

## 🔍 Шаг 5: Проверка работоспособности

### 5.1. Проверка API

Откройте в браузере:
```
https://md.ваш-домен.ru/health
```

**Ожидаемый результат:**
```json
{
  "status": "ok",
  "service": "markdown-pdf-converter"
}
```

✅ **Если видите это** - бэкенд работает!

❌ **Если ошибка 500** - проверьте логи (см. Шаг 6)

### 5.2. Проверка фронтенда

Откройте:
```
https://md.ваш-домен.ru/
```

Должна отобразиться главная страница с интерфейсом загрузки файлов.

### 5.3. Полный тест

1. Загрузите тестовый .md файл
2. Нажмите "Скачать PDF"
3. PDF должен сгенерироваться и скачаться

---

## 📊 Шаг 6: Отладка и логи

### 6.1. Просмотр логов Passenger

```bash
# Логи Passenger (если есть доступ)
tail -f ~/passenger.log
```

Или через панель ISPmanager:
1. **Инструменты → Логи**
2. Выберите ваш домен
3. Просмотрите error.log

### 6.2. Частые проблемы

#### Проблема 1: Error 500 - Internal Server Error

**Причина:** Неправильные пути в конфигурации

**Решение:**
```bash
# Проверьте пути в passenger_wsgi.py
cat ~/public_html/md/passenger_wsgi.py | grep "/home/"

# Убедитесь что u1234567 заменен на ваше имя пользователя
```

#### Проблема 2: ModuleNotFoundError: No module named 'server'

**Причина:** Python не может найти server.py

**Решение:**
```bash
# Проверьте что server.py существует
ls -la ~/public_html/md/server.py

# Проверьте права доступа
chmod 644 ~/public_html/md/server.py
```

#### Проблема 3: WeasyPrint ошибки

**Причина:** Отсутствуют системные библиотеки

**Решение:**
1. Обратитесь в техподдержку REG.RU
2. Попросите установить:
   - libpango-1.0-0
   - libpangoft2-1.0-0
   - libffi-dev

#### Проблема 4: CORS ошибки в браузере

**Причина:** CORS не настроен

**Решение:**
```bash
# Проверьте что flask-cors установлен
pip list | grep flask-cors

# Если нет, установите
pip install flask-cors
```

### 6.3. Перезапуск приложения

После изменений в коде:

```bash
# Создайте файл restart.txt
touch ~/public_html/md/tmp/restart.txt

# Или перезапустите Passenger через панель управления
```

---

## 🔐 Шаг 7: Безопасность (опционально)

### 7.1. Ограничение доступа к API

Добавьте в `.htaccess`:

```apache
# Ограничить доступ к /generate-pdf только с вашего домена
<Location "/generate-pdf">
    SetEnvIf Origin "^https://md\.ваш-домен\.ru$" ORIGIN_OK
    Header set Access-Control-Allow-Origin %{ORIGIN}e env=ORIGIN_OK
</Location>
```

### 7.2. Rate limiting

Для защиты от перегрузки установите Flask-Limiter:

```bash
pip install Flask-Limiter
```

Добавьте в `server.py`:

```python
from flask_limiter import Limiter
from flask_limiter.util import get_remote_address

limiter = Limiter(
    app=app,
    key_func=get_remote_address,
    default_limits=["100 per hour"]
)

@app.route('/generate-pdf', methods=['POST'])
@limiter.limit("10 per minute")
def generate_pdf():
    # ... существующий код
```

---

## 📈 Шаг 8: Оптимизация производительности

### 8.1. Кэширование статических файлов

Уже настроено в `.htaccess`:

```apache
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
</IfModule>
```

### 8.2. Сжатие ответов

```apache
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/css application/javascript
</IfModule>
```

### 8.3. Настройка Passenger

Создайте файл `.passenger/environment` (если доступно):

```bash
PASSENGER_MAX_POOL_SIZE=6
PASSENGER_MIN_INSTANCES=2
```

---

## 🎯 Шаг 9: Финальная проверка

### Чек-лист развертывания:

- [ ] Все файлы загружены на сервер
- [ ] Виртуальное окружение создано
- [ ] Зависимости установлены (`pip list` показывает Flask, WeasyPrint, etc.)
- [ ] `passenger_wsgi.py` обновлен с правильным путем к virtualenv
- [ ] `.htaccess` обновлен с правильными путями
- [ ] Права доступа установлены (755 для .py, 644 для остальных)
- [ ] Домен/поддомен настроен
- [ ] `/health` возвращает `{"status": "ok"}`
- [ ] Главная страница открывается
- [ ] Генерация PDF работает

---

## 🆘 Помощь и поддержка

### Техподдержка REG.RU

- **Телефон:** 8 (800) 505-36-75
- **Email:** support@reg.ru
- **Онлайн чат:** https://www.reg.ru/support/

### Полезные команды для отладки

```bash
# Проверка версии Python
python3.9 --version

# Список установленных пакетов
pip list

# Проверка структуры директорий
tree ~/public_html/md/

# Просмотр логов Apache
tail -50 ~/www/ваш-домен.ru/logs/error.log

# Проверка процессов Passenger
passenger-status
```

### Частые вопросы

**Q: Сколько времени занимает развертывание?**
A: 15-30 минут при наличии SSH доступа.

**Q: Можно ли без SSH?**
A: Да, но сложнее. Используйте веб-интерфейс панели управления для создания virtualenv.

**Q: Какой объем ОЗУ нужен?**
A: Минимум 512 MB, рекомендуется 1 GB+.

**Q: Поддерживает ли reg.ru Python 3.9?**
A: Да, на большинстве современных тарифов. Проверьте в панели управления.

---

## 📝 Краткая памятка

```bash
# 1. Подключение
ssh u1234567@ваш-домен.ru

# 2. Создание virtualenv
python3.9 -m venv ~/virtualenv/md

# 3. Активация
source ~/virtualenv/md/bin/activate

# 4. Установка зависимостей
cd ~/public_html/md/
pip install -r requirements.txt

# 5. Настройка прав
chmod 755 passenger_wsgi.py
chmod 644 server.py .htaccess

# 6. Перезапуск
touch tmp/restart.txt

# 7. Проверка
curl https://md.ваш-домен.ru/health
```

---

## ✅ Готово!

Теперь ваш Markdown Document Converter работает на REG.RU по адресу:

🌐 **https://md.ваш-домен.ru**

Приятного использования! 🎉

---

**Версия документа:** 1.0
**Дата:** 2025-11-19
**Автор:** Markdown Document Converter Team
