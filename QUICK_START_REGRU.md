# 🚀 Быстрый старт на REG.RU

Минимальная инструкция для опытных пользователей.

## За 5 минут

### 1. Загрузите файлы

```bash
# По FTP/SFTP в:
/home/u1234567/public_html/md/
```

### 2. SSH подключение

```bash
ssh u1234567@ваш-домен.ru
```

### 3. Запустите автоустановку

```bash
cd ~/public_html/md/
bash setup_regru.sh
```

**Готово!** ✅

---

## Ручная установка (если нет SSH)

### 1. Создайте виртуальное окружение

Через панель ISPmanager:
- **Сайты** → **Python** → **Создать окружение**
- Версия: Python 3.9
- Путь: `virtualenv/md`

### 2. Установите пакеты

```bash
pip install Flask==3.0.0 flask-cors==4.0.0 WeasyPrint==60.2 Pillow==10.1.0
```

### 3. Отредактируйте файлы

**passenger_wsgi.py** - замените `u1234567` на ваш логин:
```python
INTERP = os.path.expanduser("~/virtualenv/md/bin/python3")
```

**.htaccess** - замените `u1234567` на ваш логин:
```apache
PassengerAppRoot /home/u1234567/public_html/md
PassengerPython /home/u1234567/virtualenv/md/bin/python3
```

### 4. Настройте домен

ISPmanager → **WWW** → **Домены** → **Создать поддомен**
- Имя: `md`
- Директория: `/md`

---

## Проверка

```bash
# Проверка API
curl https://md.ваш-домен.ru/health

# Должно вернуть:
{"status": "ok", "service": "markdown-pdf-converter"}
```

Откройте `https://md.ваш-домен.ru/` в браузере.

---

## Перезапуск

```bash
touch ~/public_html/md/tmp/restart.txt
```

---

## Помощь

**Подробная инструкция:** `DEPLOY_REGRU.md`

**Поддержка REG.RU:** 8 (800) 505-36-75
